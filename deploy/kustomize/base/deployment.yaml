apiVersion: apps/v1
kind: Deployment
metadata:
  name: headtotails
  namespace: headscale
  labels:
    app.kubernetes.io/name: headtotails
    app.kubernetes.io/component: api-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: headtotails
  template:
    metadata:
      labels:
        app.kubernetes.io/name: headtotails
        app.kubernetes.io/component: api-proxy
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: headtotails
          image: ghcr.io/alam0rt/headtotails:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            # headscale gRPC endpoint — adjust to match your headscale Service name
            - name: HEADSCALE_ADDR
              value: "headscale:50443"
            # Tailnet name (must match the dedicated operator user)
            - name: TAILNET_NAME
              valueFrom:
                secretKeyRef:
                  name: headtotails-secrets
                  key: tailnet_name
            - name: LISTEN_ADDR
              value: ":8080"
            # Secrets injected from the headtotails-secrets Secret
            - name: HEADSCALE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: headtotails-secrets
                  key: headscale_api_key
            - name: OAUTH_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: headtotails-secrets
                  key: oauth_client_id
            - name: OAUTH_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: headtotails-secrets
                  key: oauth_client_secret
            - name: OAUTH_HMAC_SECRET
              valueFrom:
                secretKeyRef:
                  name: headtotails-secrets
                  key: oauth_hmac_secret
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 500m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
